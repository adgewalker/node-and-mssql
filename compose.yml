version: '3.9'

services:
  node-frontend:
    container_name: node-frontend
    # depends_on:
    #   - mssql-db
    build: 
      context: .
      dockerfile: DockerFile
    ports:
      - "8080:8080"
    volumes:
      - ./:/workspace:cached

